export class HeroiModel {
    nome: string;
    foto: string;
    descricao: string;

    constructor(nome: string, foto: string, descricao: string) {
        this.nome = nome;
        this.foto = foto;
        this.descricao = descricao;
    }
}
